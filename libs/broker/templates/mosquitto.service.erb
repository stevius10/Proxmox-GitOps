[Unit]
Description=Mosquitto MQTT Broker
After=network.target

[Service]
Type=simple
ExecStart=/usr/sbin/mosquitto -c <%= @config_file %>
Restart=on-failure
User=<%= @user %>
Group=<%= @group %>

[Install]
WantedBy=multi-user.target
