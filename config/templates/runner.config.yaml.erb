name: runner
labels: ["<%= node['runner']['conf']['label'] %>"]

log:
  level: warn

runner:
  file: .runner
  capacity: 1
  timeout: 1h
  labels: ["<%= node['runner']['conf']['label'] %>"]

cache:
  enabled: false
  dir: "<%= node['runner']['dir']['cache'] %>"

host:
  workdir_parent: "<%= node['runner']['dir']['cache'] %>"
