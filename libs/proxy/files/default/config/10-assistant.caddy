# *.local.caddy (.gitignore): *.* {
#   import default api 192..
#   tls /share/.certs/.lego/certificates/*.*.crt /share/.certs/.lego/certificates/*.*.key
# }

# Home Assistant Proxy Configuration
# http: trusted_proxies: - <proxy> # e. g. 192.168.178.101

route {
    reverse_proxy @internal {vars.upstream}:8123
}
