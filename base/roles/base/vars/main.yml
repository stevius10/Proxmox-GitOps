default_packages:
  - vim
  - wget
  - sudo
  - curl
  - unzip

default_groups: ["config"]

default_users:
  - { name: "app",  groups: ["config"], create_home: false, home: "/app" }
  - { name: "config", groups: ["config", "root", "sudo"], create_home: false, home: "/app" }

ssh_users:
  - config
