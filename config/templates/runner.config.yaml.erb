name: runner
labels: ["<%= node['runner']['conf']['label'] %>"]

log:
  level: warn

runner:
  file: .runner
  capacity: 1
  timeout: 1h
  labels: [ "<%= node['runner']['conf']['label'] %>" ]
  envs:
    ACT_DISABLE_CACHE: "true"

cache:
  enabled: false
