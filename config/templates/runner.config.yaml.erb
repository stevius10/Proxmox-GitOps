name: <%= node['hostname'] %>
labels: ["<%= node['runner']['labels'] %>"]

log:
  level: warn

runner:
  file: .runner
  capacity: 1
  timeout: 1h
  labels: ["<%= node['runner']['labels'] %>"]

cache:
  enabled: false
  dir: "<%= node['runner']['dir']['cache'] %>"

host:
  workdir_parent: "<%= node['runner']['dir']['cache'] %>"
