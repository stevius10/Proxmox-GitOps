<% @hosts.each do |entry| -%>
  <% domain, ip = entry.split(' ') -%>
  <%= domain %> {
  reverse_proxy <%= ip %>
  log {
  output file <%= @log_dir %>/<%= domain %>.log
  format single_field common_log
  }
  }
<% end -%>
